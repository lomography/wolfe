version: 2.1

jobs:
  build:
    docker:
      - image: lomography/wolfe:1.0
        environment:
          RAILS_ENV: test
          RUBY_VERSION: 2.6.6
          BUNDLER_VERSION: 2.1.4
    resource_class: small
    steps:
      - checkout
      - run:
          name: Run rails tests
          command: bundle exec rake


